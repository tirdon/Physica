// Typesetting — the product umbrella. One `import Typesetting` re-exports the
// pure typesetting stack: the numeric/geometry Foundation it builds on, the
// Algebra CAS, and the Font/MathSVG parsers plus the Literals. It carries no
// scene/entity coupling, so it is reusable on its own.

@_exported import PhysicaFoundation
@_exported import PhysicaAlgebra
@_exported import PhysicaTypesetting
